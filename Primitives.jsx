export function Button({ children, variant = 'primary', className = '', ...props }) { const styles = { primary: 'bg-blue-700 text-white hover:bg-blue-800', secondary: 'border border-slate-300 bg-white text-slate-700 hover:bg-slate-50', danger: 'bg-red-600 text-white hover:bg-red-700', ghost: 'text-slate-600 hover:bg-slate-100' }; return <button className={`rounded-xl px-4 py-2.5 text-sm font-semibold transition disabled:cursor-not-allowed disabled:opacity-50 ${styles[variant]} ${className}`} {...props}>{children}</button>; }
export function Card({ children, className = '' }) { return <div className={`rounded-2xl border border-slate-200 bg-white shadow-sm ${className}`}>{children}</div>; }
export function Field({ label, error, className = '', ...props }) { return <label className={`block text-sm font-medium text-slate-700 ${className}`}>{label}{props.type === 'textarea' ? <textarea className="form-input min-h-28" {...props} /> : <input className="form-input" {...props} />}{error && <span className="mt-1 block text-xs text-red-600">{error}</span>}</label>; }
export function SelectField({ label, children, ...props }) { return <label className="block text-sm font-medium text-slate-700">{label}<select className="form-input" {...props}>{children}</select></label>; }
export function StatusBadge({ status }) { const styles = { approved: 'bg-emerald-50 text-emerald-700', shortlisted: 'bg-blue-50 text-blue-700', applied: 'bg-amber-50 text-amber-700', pending: 'bg-amber-50 text-amber-700', rejected: 'bg-red-50 text-red-700', closed: 'bg-slate-100 text-slate-600', hired: 'bg-purple-50 text-purple-700' }; return <span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${styles[status] || 'bg-slate-100 text-slate-600'}`}>{status}</span>; }
export function PageHeader({ eyebrow, title, description, actions }) { return <div className="mb-8 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div>{eyebrow && <p className="text-xs font-bold uppercase tracking-[0.18em] text-orange-600">{eyebrow}</p>}<h1 className="mt-2 text-3xl font-bold tracking-tight text-slate-950">{title}</h1>{description && <p className="mt-2 max-w-2xl text-sm text-slate-500">{description}</p>}</div>{actions && <div className="flex gap-2">{actions}</div>}</div>; }
export function SectionTitle({ children }) { return <h2 className="text-lg font-bold text-slate-900">{children}</h2>; }
